import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev-mode indicator badge overlaps the game's bottom action bar.
  devIndicators: false,
};

export default nextConfig;
